<div class="tracks-container">
    <div class="track-header">
        <h2>Title</h2>
        <h2>Listen</h2>
    </div>
    {{#each albumTracks}}
    <div class="tracks">
        <h3>{{this.name}}</h3>
        <audio controls src="{{this.preview_url}}"></audio>
    </div>
    {{/each}}
</div>